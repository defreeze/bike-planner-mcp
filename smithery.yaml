name: bike-planner-mcp
version: 1.0.0
description: Modular trip planning MCP for Smithery demo
maintainer: your-name <your-email@example.com>

endpoints:
  tools: /tools
  manifest: /mcp.json
  run: /run

entrypoint: app:app

startCommand:
  docker: docker build -t bike-planner-mcp . && docker run -p 8000:8000 bike-planner-mcp
  shell: uvicorn app:app --host 0.0.0.0 --port 8000 